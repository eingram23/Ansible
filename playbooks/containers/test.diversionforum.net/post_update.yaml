---
- name: Set code permissions after update
  hosts: "{{ hostvar|default([]) }}"
  gather_facts: no
  become_user: poduser
  become_method: sudo
  
  vars:
    app: 'test.diversionforum.net'
    root: '/home/poduser/code/'

  roles:
    - post_update
...
  